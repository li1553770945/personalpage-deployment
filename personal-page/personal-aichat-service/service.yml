apiVersion: v1
kind: Service
metadata:
  name: personal-aichat-service-service
spec:
  type: ClusterIP
  selector:
    app: personal-aichat-service-pod  # 确保与 Pod 的标签匹配
  ports:
  - protocol: TCP
    port: 8884    # Service暴露的端口
    targetPort: 8894 # 转发到容器的端口
  
